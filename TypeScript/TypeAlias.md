# Type Alias

⚡️ Type alias is a way to give a name to a type, which can reduce duplicated code








``` typescript 
type Point = {
    x: number;
    y: number;
};
let point: Point = {
    x: 10,
    y: 20
};


```

