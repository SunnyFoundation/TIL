# any








``` typescript 
// it can be all of type 
let randomValue: any = 42;

randomValue = 'Now I am a string';

randomValue = true; // Now I am a boolean

randomValue= { key: 'value' }; // Now I am an object

randomValue = [1, 2, 3]; // Now I am an array
```

